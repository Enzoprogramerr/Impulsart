---

interface Props{
    text: string;
    href:string;
    variant: |"primary" 
            |"secondary"
            |"cta"
            |"outline";

}
const {text, href, variant} = Astro.props
---

<a 
    href={href}
    class={`btn btn-${variant}`}
>
    {text}
</a>